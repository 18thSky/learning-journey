#include <stdio.h>
//function to convert Celsius to Fahrenheit
float temp(float c);

float temp(float c){
    return ((9.0/5.0)*c)+32.0;
}

int main(){
    float c=45.0;
    printf("The conversion of Celsius to Fahrenheit for %f is %.2f",c,temp(c));
    return 0;
}
