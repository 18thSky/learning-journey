#include <stdio.h>
// Function to calculate force of attraction on a body of mass 'm' exerted by earth (g=9.8m/s*2)
float force(float);

float force(float mass){
    return mass * 9.8;
}
int main(){
    int m=45;
    printf("The Value of force is %.2f\n",force(m));
    return 0;
}
